<p id="notice"><%= notice %></p>

<main role="main">
<div class="container">
  <br>
  <h1 class='text-center'>About me</h1>
  <p class="lead text-muted", 'text-center'>
    Welcome to my awesome portfolio. This showcases my programming skills.
  </p>
</div>
</main>

<body>
  <div class="album py-5 bg-light">
    <h2 class='text-center'>Projects</h2>
    <div class='text-right'>
      <%= link_to 'New Project', new_project_path, class: "btn btn-success" %>
    </div>
    <br>
    <div class="container">
      <div class="row">
        <% @projects.each do |project| %>
          <tr>
            <div class="col-md-4">
              <div class="card mb-4 box-shadow">
                <div class="card-body">
                  <h5 class="card-title">
                    <%= project.title %>
                  </h5>
                  <p class="card-text text-truncate">
                    <%= project.description %>
                  </p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group">
                      <%= link_to "Show", project, class: "btn btn-primary" %>
                      <%= link_to 'Edit', edit_project_path(project), class: "btn btn-info" %></td>
                      <%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </tr>
        <% end %>
      </div>
    </div>
  </div>
</body>